import { Color } from './color';
import { listaColores } from './listaColores';
import { motion } from 'framer-motion';

//?????????????????????????????????????????????????????????????????
//?????????????????????????????????????????????????????????????????
//?????????????????????????????????????????????????????????????????

export const Colores = ({ idCarpeta }) => {
	const variants = {
		visible: {
			'align-self': 'center',
			justifyItems: 'center',
			'overflow-y': 'auto',
			'row-gap': '5%',
			
			height: '75%',
			padding: '5px',
			position: 'absolute',
			transition: { duration: 0.3 },
			width: '80%',

			'grid-template-columns': 'repeat(5, minmax(5px, 50px))',
			display: 'grid'
		},
		active: {}
	};

	return (
		<motion.ul variants={variants} key={idCarpeta} animate="visible">
			{listaColores.map((color) => (
				<Color
					color={color.color}
					id={color.id}
					key={color.id}
					nombreColor={color.nombreColor}
					colorBooleano={color.colorBooleano}
				/>
			))}
		</motion.ul>
	);
};
