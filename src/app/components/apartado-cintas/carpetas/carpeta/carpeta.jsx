
import { BtnMenuColores } from './BtnMenuColores';
import { BtnBorrar } from '../../btn-borrar';
import { CantidadColor } from './CantidadColor';
import { BtnRestar } from './btn-restar';
import { BtnSumar } from './btn-sumar';
import { motion } from 'framer-motion';

//?????????????????????????????????????????????????????????????????
//?????????????????????????????????????????????????????????????????
//?????????????????????????????????????????????????????????????????

export const Carpeta = ({ idCarpeta }) => {

	const variants = {
		visible: {
			borderRadius: '15px',
			justifySelf: 'center',
			background: 'radial-gradient(var(--color-6), var(--color-4))',
			height: '135px',
			margin: '3%',
			overflow: 'hidden',
			position: 'relative',
			transform: 'scale(1)',
			transition: { duration: 0.3, type: 'spring' },
			width: '90%',

			display: 'grid'
		},
		exit: {
			transform: 'scale(0)',
			transition: { duration: .2, delay: idCarpeta * .02, type: 'spring' }
		}
	};

	return (
		<motion.li
			id={`carpeta-${idCarpeta}`}
			key={idCarpeta}
			variants={variants}
			initial={{
				...variants.visible,
				transform: 'scale(0)',
			}}
			animate="visible"
			exit="exit"
		>
			<BtnMenuColores idCarpeta={idCarpeta} idEspecialCarpeta={'idCarpeta' + idCarpeta} />
			<BtnBorrar idCarpeta={idCarpeta} />
			<CantidadColor idCarpeta={idCarpeta} />
			<BtnRestar idCarpeta={idCarpeta} />
			<BtnSumar idCarpeta={idCarpeta} />
		</motion.li>
	);
};
